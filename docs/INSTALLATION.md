# 📦 Installation Guide / 安装手册

Animal Island is a standard Hexo theme. You can install it into any Hexo site's `themes/` directory by cloning from GitHub, copying the source folder, or using a local symlink.

Animal Island 是标准 Hexo 主题，可以通过 GitHub 克隆、源码复制或本地软链接安装到任意 Hexo 项目的 `themes/` 目录中。

> ⚠️ Usage restriction / 使用限制：This project is for personal learning, research, and non-commercial demonstration only. Commercial use is prohibited. Read [LEGAL_NOTICE.md](../LEGAL_NOTICE.md) before using it. / 本项目仅限个人学习、研究与非商业演示使用，严禁商业使用。安装和使用前请阅读 [LEGAL_NOTICE.md](../LEGAL_NOTICE.md)。

## 🌿 Option 1: Clone from GitHub / 方式一：从 GitHub 克隆

Recommended for most users.

推荐大多数用户使用这种方式。

```bash
cd /path/to/your-hexo-site
mkdir -p themes
git clone https://github.com/timoforge/hexo-theme-animal-island.git themes/animal-island
```

Enable the theme in the Hexo site root `_config.yml`.

然后修改 Hexo 站点根目录 `_config.yml`：

```yaml
theme: animal-island
```

## 🧺 Option 2: Copy downloaded source / 方式二：复制已下载源码

Use this when installing from a release archive or another local source folder.

适合从 Release 压缩包或本机源码目录安装：

```bash
cd /path/to/your-hexo-site
mkdir -p themes
cp -a /path/to/hexo-theme-animal-island themes/animal-island
```

## 🔗 Option 3: Symlink for local development / 方式三：本地开发软链接

Use this when maintaining the theme locally and testing it in one or more Hexo sites.

适合在同一台机器上维护主题并同步测试多个 Hexo 项目：

```bash
cd /path/to/your-hexo-site
mkdir -p themes
ln -s /path/to/hexo-theme-animal-island themes/animal-island
```

Changes to `/path/to/hexo-theme-animal-island` will affect every Hexo site using this symlink.

注意：软链接方式下，修改 `/path/to/hexo-theme-animal-island` 会影响所有使用该链接的站点。

## ⚙️ Add theme override config / 添加主题覆盖配置

Copy the example override config to the Hexo site root.

推荐复制示例配置到站点根目录：

```bash
cp themes/animal-island/examples/_config.animal-island.yml _config.animal-island.yml
```

Hexo merges the theme default `_config.yml` with the site-level `_config.animal-island.yml`; the site-level file has higher priority. Keep personal settings in the site root so theme upgrades do not overwrite them.

Hexo 会把主题默认 `_config.yml` 和站点根目录 `_config.animal-island.yml` 合并，后者优先级更高。升级主题时，尽量只保留站点根目录中的覆盖配置，不要直接改主题默认配置。

## 🗺️ Recommended pages / 推荐创建页面

The default menu includes search, categories, tags, archives, and about. Archives are generated by Hexo, but the other pages should be created manually.

默认菜单包含搜索、分类、标签、归档和关于。归档由 Hexo 内置生成，其他页面建议手动创建：

```bash
hexo new page search
hexo new page categories
hexo new page tags
hexo new page about
```

Set `type` in each page's front matter.

然后给对应页面设置 `type`：

```yaml
# source/search/index.md
---
title: Search
type: search
---
```

```yaml
# source/categories/index.md
---
title: Categories
type: categories
---
```

```yaml
# source/tags/index.md
---
title: Tags
type: tags
---
```

中文站点可以把 `title` 改成 `搜索`、`分类`、`标签`。

## 🧪 Build verification / 生成验证

Run Hexo generation from the Hexo site root.

在 Hexo 站点根目录运行：

```bash
hexo clean && hexo generate
```

If the site already defines its own build script, you can use that instead.

如果站点 `package.json` 已经定义了构建命令，也可以使用：

```bash
npm run build
```

## 📁 Theme folder name rule / 目录名规则

The Hexo `theme:` value must match the folder name under `themes/`.

`theme:` 的值必须等于 `themes/` 下的目录名。

Recommended / 推荐：

```text
themes/animal-island
```

```yaml
theme: animal-island
```

If you keep the folder name `hexo-theme-animal-island`, configure Hexo like this.

如果你保留目录名 `hexo-theme-animal-island`，则需要：

```yaml
theme: hexo-theme-animal-island
```

The override config file name must match too.

同时覆盖配置文件名也要改成：

```text
_config.hexo-theme-animal-island.yml
```
